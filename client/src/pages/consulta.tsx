import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function Consulta() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Consulta</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is the Consulta page.</p>
      </CardContent>
    </Card>
  );
}
