import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function Agente() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Agente</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is the Agente page.</p>
      </CardContent>
    </Card>
  );
}
